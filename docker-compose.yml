# docker-compose.yml — complete dev stack: postgres + MCP server + frontend
# Requires .env file (see .env.example) with secrets like POSTGRES_PASSWORD
# and GOOGLE_MAPS_API_KEY.  Compose auto-loads .env for ${VAR} substitution.

services:
  tw-prop-postgres:
    build:
      context: .
      dockerfile: Dockerfile.postgres
    image: tw-prop-postgres:latest
    container_name: tw-prop-postgres
    hostname: tw-prop-postgres
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-prop}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-prop_dev_only}
      POSTGRES_DB: ${POSTGRES_DB:-prop}
      POSTGRES_INITDB_ARGS: "--encoding=UTF-8 --locale=C"
    ports:
      - "5432:5432"
    volumes:
      - ./pg_data:/var/lib/postgresql/data
      - ./initdb:/docker-entrypoint-initdb.d
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U prop -d prop"]
      interval: 5s
      timeout: 3s
      retries: 10
      start_period: 10s
    restart: unless-stopped

  tw-prop-mcp:
    build:
      context: .
      dockerfile: Dockerfile
    image: tw-prop-mcp:latest
    container_name: tw-prop-mcp
    hostname: tw-prop-mcp
    depends_on:
      tw-prop-postgres:
        condition: service_healthy
    environment:
      DATABASE_URL: ${DATABASE_URL}
      MCP_TRANSPORT: ${MCP_TRANSPORT:-http}
      MCP_HTTP_ADDR: ${MCP_HTTP_ADDR:-:8080}
      DEFAULT_SNAPSHOT_VERSION: latest
      ALGORITHM_VERSION: comparable-v2.0
      CONFIGURATION_VERSION: v2.0
      LOG_LEVEL: ${LOG_LEVEL:-info}
      # Auto freshness check + scheduled refresh (Boss: 1/11/21 發布，週末順延 3 天窗口，6h 檢查避免單次錯過)
      AUTO_REFRESH_ENABLED: ${AUTO_REFRESH_ENABLED:-true}
      DATA_FRESHNESS_DAYS: ${DATA_FRESHNESS_DAYS:-10}
      DATA_REFRESH_INTERVAL: ${DATA_REFRESH_INTERVAL:-6h}
      DATA_IMPORT_URL: ${DATA_IMPORT_URL:-}
      AUTO_DISCOVER: ${AUTO_DISCOVER:-true}
    ports:
      - "8080:8080"
    restart: unless-stopped

  tw-prop-frontend:
    build:
      context: .
      dockerfile: Dockerfile.frontend
      args:
        VITE_GOOGLE_MAPS_API_KEY: ${GOOGLE_MAPS_API_KEY}
    environment:
      MCP_SERVER_URL: ${MCP_SERVER_URL:-/mcp}
      MAP_PROVIDER: ${MAP_PROVIDER:-leaflet}
    image: tw-prop-frontend:latest
    container_name: tw-prop-frontend
    hostname: tw-prop-frontend
    depends_on:
      tw-prop-mcp:
        condition: service_started
    ports:
      - "80:80"
    restart: unless-stopped
